import Launchbrowser from './Launchbrowser';

class Login extends BasePage {
    constructor() {const selectors = {
    LOGIN_EMAIL_INPUT: '[name="email"]',
    LOGIN_PASSWORD_INPUT: '[name="password"]',
  } } } 